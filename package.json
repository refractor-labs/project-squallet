{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "prettier": "lerna run prettier",
    "test": "lerna run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
