// this is the chain where the pkp nft lives.
export const chainLit = 'chronicle'
//lit protocol network
export const litNetwork = 'serrano'

// chain for generating the auth sigs on
export let walletNetwork = 'mumbai'
